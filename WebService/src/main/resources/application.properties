spring.application.name=WebService
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=ReFire_DB_1

# Shared secret for the /admin/** endpoints. The "changeme" fallback is only
# for local development - production MUST set a real ADMIN_KEY env var.
app.admin-key=${ADMIN_KEY:changeme}